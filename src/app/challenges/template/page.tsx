import {ReactElement} from "react";
import './style.css';

export default function Template(): ReactElement {
    return (
        <main className='w-full min-h-screen bg-dvx-green text-white'>
        </main>
    );
}